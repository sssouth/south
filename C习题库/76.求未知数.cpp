//题目：809*??=800*??+9*?? 其中??代表的两位数, 809*??为四位数，8*??的结果为两位数，9*??的结果为3位数.求:??代表的两位数,及809*??后的结果.
#include <stdio.h>
int main(void)
{
	int X;//X表示?? 
	for(X=10;X<13;X++)//由题意9*??为3位数，故Xw为10或11或12 
	{
		if(9*X>=100 && 9*X<1000)
			break;
	}
	printf("??代表%d,809*??=%d",X,X*809);
	return 0;
}
